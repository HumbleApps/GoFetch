import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {},
    buttonContainer: {
        marginTop: 0,
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-around',
    },
})